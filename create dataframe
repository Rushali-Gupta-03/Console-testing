
s1 = pd.Series([1, 3, 4, 5, 6, 2])   
  
       # Define series 2       
s2 = pd.Series([1.1, 3.5, 4.7, 5.8, 2.9, 9.3]) 
  
      # Define series 3
s3 = pd.Series(['a', 'b', 'c', 'd', 'e','f'])     

    # Define Data
Data ={'first':s1, 'second':s2, 'third':s3} 

#     # Create DataFrame
df = pd.DataFrame(Data)   
print(df)
# # create(dfseries)
f.save_table(df,name="Base Report")
f.create_df(df,preferred_name="Console Dataframe")
